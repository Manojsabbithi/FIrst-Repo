pipeline{
    agent any
    stages{
        stage('Maven'){
            steps{
                echo "Welcome to Maven, printing Maven version"
                sh 'mvn --version'
            }
            
        }
    }
}
