pipeline{
    agent any
    stages{
        stage('FirstStage'){
            steps{
                sh 'hostname -i'
            }
        }
        stage('SecondStage'{
            steps{
                sh 'hostname -i'
            }
        })
    }
}
